# Environment variables (optional)
# The Gemini API key is configured in the app's Settings modal
# and stored in browser localStorage

# If you want to pre-configure the API key (optional):
# VITE_GEMINI_API_KEY=your_api_key_here
